# Scripts

This directory contains utility scripts for the Rinzler project.

Currently, no scripts are present. Version management is done manually.

## Version Management

Versions are managed manually by editing the workspace `Cargo.toml` and updating dependency versions as needed.

See `RELEASE.md` in the project root for the full release workflow.
